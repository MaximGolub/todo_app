class Task {
  constructor({ id, title, description, status, createdDate }) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.status = status;
    this.createdDate = createdDate;
  }
}

export default Task;
